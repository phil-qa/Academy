Programming
 
 
112.In the following line of Python, which word represents the builtin function?  
print(“The square root of ”, value, “is “, math.sqrt(value))Required to answer. Single choice.
(1 Point)
print
math
value
sqrt

113.Which Python data collection/type is this?
(10, 20, 30, 40)Required to answer. Single choice.
(1 Point)
tuple
array
list
dictionary

114.What is the output from the following Python code extract?
for i in range(5):  
    print(i ** 2, end=",") Required to answer. Single choice.
(1 Point)
0,1,4,9,16,
1,4,9,16,25,
0,2,4,6,8,
2,4,6,8,10,

115.What is the output from the following Python code extract?
cats = ['Phil', ['Ron', 'Jess'], 'Frank'] 
print(cats[1][1][1]) Required to answer. Single choice.
(1 Point)
e
Phil
P
Jess

116. In Python what type is the variable c when c is asigned a value : c = 1.3? Required to answer. Single choice
(1 point)
Int
Bool
Float
Char

117 what is the output from the following Python code:
[val for val in range(10) if val%2 == 0]? Required to answer. Single choice
(1 point)
[0,1,2,3,4,5,6,8,9]
[2]
[0,2,4,6,8]
[2,4,6,8]

118 Given the following function definition in python, identify the legitemate function calls:
def calculate_tax(amount_to_tax, vat = 20): Required to answer. Multiple choice
(1 point)
calculate_tax(1000)
calculate_tax(vat=14,1000)
calculate_tax(1000,14)
calculate_tax(1000, vat=14)

119.What does the len() function return when called on an empty list [] in Python? Required to answer. Single choice
(1 point)
0
1
None
Error

120.Which of the following best describes the behavior of the enumerate() function in Python? Required to answer. Single choice
(1 point)

It returns a list of tuples containing indices and values from an iterable.
It generates an iterator that yields tuples of indices and values from an iterable.
It maps a function to each element of an iterable, returning a list of results.
It reverses the order of elements in an iterable and returns the modified iterable.

121. Which of the following statements correctly initializes an empty dictionary in Python? Single choice
(1 point)
dict = {}
dict = dict()
dict = {()}
dict = ()